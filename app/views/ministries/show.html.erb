<div><%= @ministry.name %></div>
<div><%= @ministry.description %></div>

<h2>Department List of this ministry</h2>
<% @departments.each do |d| %>
	<%=link_to d.name, department_path(@ministry,d) %>
	<br>
<% end %>

<br>
<%= link_to "Add Department", new_department_path(@ministry) %>